# Repo-root env vars are read by docker-compose.
# Backend-specific vars live in backend/.env (copied from backend/.env.example).
# Candidate: copy this to .env if you need to override defaults.

COMPOSE_PROJECT_NAME=platform-interview
